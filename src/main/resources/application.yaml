spring:
  application:
    name: gestioncabinet
  datasource:
    url: jdbc:postgresql://localhost:5433/cabinet_medical
    username: postgres
    password: "Aya2005"
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  mail:
    host: smtp.gmail.com
    port: 587
    username: ayaazzouzi39@gmail.com
    password: "zunzcyhrxiiyfflv"   # mot de passe d'application
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE
