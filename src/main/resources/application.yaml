spring:
  application:
    name: cabinet_medical
  datasource:
    # ⚠️ Changement 1 : URL du Pooler (port 6543) + prepareThreshold=0 (OBLIGATOIRE)
    url: jdbc:postgresql://aws-1-eu-west-1.pooler.supabase.com:6543/postgres?prepareThreshold=0&sslmode=require

    # ⚠️ Changement 2 : Ton username complet fourni par Supabase
    username: postgres.jajzouqlzddoifzuztyl

    password: "Omaima2004"
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        # Optimisation recommandée pour Postgres
        jdbc:
          lob:
            non_contextual_creation: true

server:
  port: 8080

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE